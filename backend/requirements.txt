# ── Web framework ──────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
websockets==12.0
python-multipart==0.0.9

# ── Auth ───────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1          # passlib 1.7.4 breaks with bcrypt>=4.1 (72-byte test vector)

# ── LLM / Ollama ───────────────────────────────────────────
ollama==0.6.2

# ── Vector memory ──────────────────────────────────────────
chromadb>=1.5.9      # 1.x supports numpy 2.x (0.5.x used np.float_ removed in numpy 2.0)

# ── Structured DB ──────────────────────────────────────────
sqlalchemy==2.0.30
aiosqlite==0.20.0

# ── File parsing ───────────────────────────────────────────
pymupdf==1.24.3
pytesseract==0.3.10
Pillow==10.3.0

# ── Scheduling ─────────────────────────────────────────────
apscheduler==3.10.4

# ── Search / retrieval ─────────────────────────────────────
rank_bm25==0.2.2
sentence-transformers==2.7.0   # cross-encoder reranker (ms-marco-MiniLM-L-6-v2)

# ── Voice (STT + TTS) ──────────────────────────────────────
faster-whisper>=1.0.0      # STT — CTranslate2-optimised Whisper (base.en, ~290 MB)
kokoro-onnx>=0.4.0         # TTS — kokoro-82M via ONNX Runtime (bm_lewis, ~310 MB)
av>=12.0.0                 # PyAV — WebM/Opus decode, bundles FFmpeg DLLs on Windows
soundfile>=0.12.0          # WAV encode for TTS output

# ── Utilities ──────────────────────────────────────────────
pydantic>=2.9.0,<3.0.0    # ollama>=0.6 requires pydantic>=2.9
pydantic-settings==2.2.1
python-dotenv==1.0.1
aiofiles==23.2.1
